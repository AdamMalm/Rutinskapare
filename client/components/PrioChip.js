import { View, Text } from "react-native";

const PrioChip = () => {
  return (
    <View className="py-2 px-4 rounded-full bg-orange mb-2">
      <Text className="text-xs font-bold">Prio</Text>
    </View>
  );
};

export default PrioChip;
